apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai-paralegal

resources:
- namespace.yaml
- app-deployment.yaml
- configmap.yaml
- secrets.yaml
- postgres-deployment.yaml
- redis-deployment.yaml
- qdrant-deployment.yaml
- ingress.yaml

# This section can be used to patch the base manifests for different environments
# For example, to change the image tag for production:
# images:
# - name: your-registry/ai-paralegal
#   newTag: v1.2.3

# Or to increase the replica count:
# patchesStrategicMerge:
# - |-
#   apiVersion: apps/v1
#   kind: Deployment
#   metadata:
#     name: ai-paralegal-app
#   spec:
#     replicas: 3 
